import { MapLeaderBoardDisplayDataPipe } from './map-leader-board-display-data.pipe';

describe('MapLeaderBoardDisplayDataPipe', () => {
  it('create an instance', () => {
    const pipe = new MapLeaderBoardDisplayDataPipe();
    expect(pipe).toBeTruthy();
  });
});
